<?php

namespace {{ namespace }};

use Illuminate\Database\Eloquent\Model;
{{ queuedImport }}use Nccirtu\Tablefy\Kanban\Contracts\KanbanAction;
use Nccirtu\Tablefy\Kanban\KanbanTransition;

/**
 * @generated by `php artisan make:tablefy-kanban-action {{ name }}`
 *
 * Runs when a card enters/leaves a column (register in the controller's
 * kanban(): ->onEnter('won', {{ name }}::class)). {{ queuedNote }}
 */
class {{ name }} implements KanbanAction{{ queuedImplements }}
{
    public function handle(Model $record, KanbanTransition $transition): void
    {
        // $transition->from, $transition->to, $transition->request
        // TODO: dispatch jobs, send notifications, set timestamps, …
    }
}

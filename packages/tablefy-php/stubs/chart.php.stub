<?php

namespace {{ namespace }};

use Illuminate\Http\Request;
{{ modelImport }}use Nccirtu\Tablefy\Charts\ChartWidget;

/**
 * @generated by `php artisan make:tablefy-chart {{ name }}`
 *
 * Data is computed here on the backend, deferred to the page, and rendered by
 * <TablefyCharts> (config-driven shadcn/recharts). Edit data()/config() freely.
 */
class {{ name }} extends ChartWidget
{
    protected string $type = '{{ type }}';

    protected ?string $heading = '{{ heading }}';

    protected ?string $description = '{{ description }}';

    protected string $xKey = '{{ xKey }}';
{{ seriesProp }}{{ optionsProp }}
    /** @return array<int, array<string, mixed>> */
    public function data(Request $request): array
    {
        return [
{{ dataBody }}
        ];
    }

    /** @return array<string, array{label: string, color?: string}> */
    public function config(): array
    {
        return [
{{ configBody }}
        ];
    }
}

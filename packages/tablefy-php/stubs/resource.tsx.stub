// @generated (Grundgerüst) – danach frei editierbar.
//
// Zentrale Meta-Daten + Routen der Resource an einer Stelle.
//
// Die Routen kommen aus Wayfinder und werden **beim Aufruf** ausgewertet, nicht
// beim Import. Nur so tragen sie die per `setUrlDefaults()` gesetzten
// URL-Defaults (z. B. den Standort-Slug) — ein beim Import berechneter String
// wäre je nach Ladereihenfolge ohne Präfix.

import {{ singular }}Controller from "@/actions/App/Http/Controllers/Tablefy/{{ singular }}Controller";

export const {{ singular }}Resource = {
  name: "{{ singular }}",
  label: "{{ singular }}",
  pluralLabel: "{{ plural }}",

  /** "page" = eigene Create/Edit-Seiten, "modal" = Dialoge. */
  formMode: "{{ formMode }}" as "page" | "modal",

  routes: {
{{ resourceRoutes }}
  },
} as const;

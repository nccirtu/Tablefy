// @generated Kanban-Schema – ab hier FREI editierbar.
//
// Spalten/Pipeline + Drag; der Karten-INHALT kommt aus dem geteilten
// `{{ singularCamel }}CardContent` (s. {{ singular }}CardContent.tsx), das auch die
// Card-Grid-Ansicht (<ServerCards />) nutzt. `groupBy` muss zum Controller-
// `kanban()->groupBy(...)` passen.

import { KanbanSchema } from "@nccirtu/tablefy-v2/kanban";
import { {{ singularCamel }}CardContent } from "./{{ singular }}CardContent";
import type { {{ singular }} } from "@/types/tablefy/{{ singularKebab }}";

export const {{ singularCamel }}Card = KanbanSchema.make<{{ singular }}>()
    .groupBy("{{ kanbanGroupBy }}")
    .columns([
{{ kanbanColumns }}
    ])
    .sortable() // Reorder innerhalb der Spalte (persistiert `position`)
    .columnsMovable() // Spalten per Drag umsortieren (nur UI)
    .card({{ singularCamel }}CardContent)
    .build();

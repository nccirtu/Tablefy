<?php

namespace {{ namespace }};

use Illuminate\Database\Eloquent\Model;
use Nccirtu\Tablefy\Relations\RelationManager;

/**
 * @generated by `php artisan make:tablefy-relation {{ resourceSingular }} {{ relation }} --modal`
 *
 * CRUD runs through {{ resourceSingular }}->{{ relation }}() — the FK is set
 * automatically by the relationship, so it is not part of rules().
 */
class {{ managerName }} extends RelationManager
{
    protected string $relation = '{{ relation }}';

    public function rules(?Model $record = null): array
    {
        return [
{{ rules }}
        ];
    }
}

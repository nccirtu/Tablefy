// @generated (vorbefüllt aus dem DB-Schema) – ab hier FREI editierbar.
//
// Für Create UND Edit. Validierungs-Wahrheit liegt im Backend ({{ singular }}Controller::rules()).
//
// Felder + Layout in einem Zug: Felder leben direkt in .schema([...]). Zum
// Gruppieren in Cards: Section.make("Titel").columns(2).schema([ …felder… ])
// (Sections aus "@nccirtu/tablefy-v2/forms" importieren), optional FormRow für Zeilen.

import {
  FormSchema,
  TextInput,
  Textarea,
  Select,
  Toggle,
  DatePicker,
} from "@nccirtu/tablefy-v2/forms";
import type { {{ singular }} } from "@/types/tablefy/{{ singularKebab }}";

export const {{ singularCamel }}Form = FormSchema.make<{{ singular }}>()
  .columns(2)
  .schema([
{{ formFields }}
  ])
  .actions((a) =>
    a.submit({ label: "Speichern" }).cancel({ label: "Abbrechen", href: "/{{ routeSlug }}" }),
  )
  .build();
